# Session handoff — 2026-09-10 (for the next Opus session)

Start the new session **in `C:\dev\swietlik`** (project agents, `.mcp.json`,
hooks and the task router all key off this root). Read `CLAUDE.md` §1a first,
as always.

## Where everything stands

| Track | State |
|---|---|
| **Phase 0 + Phase 1** | DONE and merged to `main` (PR #1, PR #6). 408 tests, lint 0 errors, CI green. MCP: 23 tools work end-to-end against the live app (recorded evidence in `docs/swietlik/evidence/`). |
| **Engine hardening** | Landed on `main` + `phase-2-design`: patch-collision fix, genCueId, NaN position, load_show restore, **fixture-id save/load stability + `modeNam` typo** (commit `0197c36` — real data-loss bugs, fixed + regression-tested). |
| **Phase 2 design** | COMPLETE on branch `phase-2-design` (pushed): spec `docs/superpowers/specs/2026-09-10-phase2-instrument-design.md` (rulings bind), consultant scope draft (`phase2-scope-draft.md`, incl. MIDI §7), architecture research (`phase2-architecture-options.md`). |
| **Phase 2 plans** | **Plans A and B are INCOMPLETE DRAFTS** (drafting agents killed by rate limit; banners at the top of each say exactly what's missing). Plans C/D/E (P2-4 records, P2-6 MIDI, P2-3 timeline) not yet drafted. |
| **UI design track** | `design-brief.md` + 10 curated screenshots (`evidence/ui-audit/`) delivered to Jake for his Claude Design session. Outputs feed P2-2 and Phase 4. |

## Immediate next actions (in order)

1. **Finish Plan A** (`plans/2026-09-10-phase2a-transport.md`): one opus agent
   reads spec + draft, completes missing tasks (cut off mid release-envelope;
   missing: UI routing task, transport bar, MCP command group per spec ruling
   11, verification section), runs the writing-plans self-review. Same for
   **Plan B** (check P2-5 keyboard tasks + verification present). These two can
   run in parallel.
2. **Execute Plan A, then/overlapping Plan B** per the execution topology
   (memory: `agent-execution-preferences`): dependency **chains → ONE agent
   working the chain end-to-end; independent branches → separate agents**, ≤5
   concurrent; **opus = hard/judgment, sonnet = implementing, haiku = search;
   NO fable agents — Jake ruled them too token-expensive (2026-09-10). Use an
   opus agent for the adversarial pre-merge review instead.**
3. Draft plans C (P2-4 fixture records), D (P2-6 MIDI), E (P2-3 timeline)
   **while** A/B execute (Jake's parallelism directive). E starts executing
   only after A + B land (spec sequencing).
4. Phase 2 merge gate (spec §Verification): all ACs green, `verification.md`
   §9 executed live via MCP with evidence, opus adversarial review, PR to
   `main`, close issues #2 (+#7 if MIDI ships).

## Standing directives (also in memory files)

- Ultracode/parallel-first; ≤5 agents; token-mindful; **no fable**.
- Autonomy: don't interrupt Jake; ping at phase-done; start next-phase research
  without waiting. Genuine decisions only.
- Git: stage explicit paths only (never `git add -A` with agents running);
  never commit to `develop`; ledger every upstream edit in the same change;
  commit trailers per the session's attribution block.
- Harness: Stop hooks enforce gates; lessons catalogue + task router are law;
  `npm run lint:ci` 0 errors; 408+ tests stay green.

## Open questions parked for Jake (defaults stand until answered)

1. Which MIDI controller does he own? (keyboard vs pad grid changes P2-6 defaults)
2. Blackout also zeroes real DMX output — ruled yes; confirm/veto.
3. De-scope ladder sacrifices timeline before MIDI — confirm/flip.
4. Open-Mercato "Definition of Ready" gate — adopt or skip (proposal §5).

## Environment notes

- Dev server: `npm start` from `C:\dev\swietlik` (detached; log to `%TEMP%`);
  the app must be open in a browser for MCP tools to connect (else
  `APP_NOT_CONNECTED`).
- GitHub: `KRUKMAN/swietlik`; branches: `main` (trunk), `develop` (pristine
  upstream mirror — never touch), `phase-2-design` (current work).
- Issues: #2 playback feel (Phase 2 closes), #3 UI overhaul (Phase 4), #4
  functional defects (partially fixed; re-triage), #5 screenshot race (FIXED),
  #7 MIDI (P2-6).
